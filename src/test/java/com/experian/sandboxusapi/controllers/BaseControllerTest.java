/*
 * CommercialCreditAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.experian.sandboxusapi.controllers;

import com.experian.sandboxusapi.CommercialCreditAPIClient;
import com.experian.sandboxusapi.Environment;
import com.experian.sandboxusapi.testing.HttpCallbackCatcher;
import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Rule;
import org.junit.rules.Timeout;

/**
 * Base class for all test cases.
 */
public class BaseControllerTest {
    /**
     * Test configuration.
     */
    public static final int REQUEST_TIMEOUT = 30;

    public static final double ASSERT_PRECISION = 0.01;
    
    /**
     * Global rules for tests.
     */
    @Rule
    public Timeout globalTimeout = Timeout.seconds(REQUEST_TIMEOUT);
    
    /**
     * Test fixtures,
     * Used to serve as HttpCallback and to capture request & response.
     */

    protected static HttpCallbackCatcher httpResponse;
    
    /**
     * Setup test.
     */
    @BeforeClass
    public static void setUp() throws Exception {
        httpResponse = new HttpCallbackCatcher(); 
    }

    /**
     * Tear down test.
     */
    @AfterClass
    public static void tearDown() throws Exception {
        httpResponse = null;
    }

    /**
     * Create test configuration from Environment variables.
     */
    protected static CommercialCreditAPIClient createConfigurationFromEnvironment() {
        CommercialCreditAPIClient.Builder builder = new CommercialCreditAPIClient.Builder();

        final String environment = System.getenv("COMMERCIAL_CREDIT_API_LIB_ENVIRONMENT");
        final String timeout = System.getenv("COMMERCIAL_CREDIT_API_LIB_TIMEOUT");
        final String oAuthClientId = System.getenv("COMMERCIAL_CREDIT_API_LIB_O_AUTH_CLIENT_ID");
        final String oAuthClientSecret =
                System.getenv("COMMERCIAL_CREDIT_API_LIB_O_AUTH_CLIENT_SECRET");
        final String oAuthUsername = System.getenv("COMMERCIAL_CREDIT_API_LIB_O_AUTH_USERNAME");
        final String oAuthPassword = System.getenv("COMMERCIAL_CREDIT_API_LIB_O_AUTH_PASSWORD");

        if (oAuthClientId != null && oAuthClientSecret != null && oAuthUsername != null && oAuthPassword != null) {
            builder.resourceOwnerAuthCredentials(oAuthClientId, oAuthClientSecret, oAuthUsername, oAuthPassword);
        }
        if (environment != null) {
            builder.environment(Environment.fromString(environment));
        }
        if (timeout != null) {
            builder.httpClientConfig(configBuilder -> configBuilder.timeout(
                    Long.parseLong(timeout)));
        }
        return builder.build();
    }

    
    /**
     * Create test configuration.
     */
    protected static CommercialCreditAPIClient createConfiguration() {
        // Set Configuration parameters for test execution
        CommercialCreditAPIClient config = createConfigurationFromEnvironment();
        config = config.newBuilder()
                .httpCallback(httpResponse)
                .build();

        try {
            config = config.newBuilder().oAuthToken(config.getResourceOwnerAuth()
                    .fetchToken())
                    .build();
        } catch (Exception ex) {
            ex.printStackTrace();
        }
        return config;
    }
}
